
TODO
    
    Модули 
        * отложенная загрузка модуля и зависимостей (зависимости указывать в объекте модуля) 
        	++ возможность указания условия при которых вообще нужно что-то грузить (напр. на основании существования данных в модели)
        - флаг autoexec="true" в хмл-маппере для автоматической загрузки модуля в случае, 
        	если данные для указанной цепочки получены, по-умолчанию автозагрузка выключена
        - ручная загрузка/выполнение модуля Follow.module('name').load([force=false]), аргумент force позволяет выполнять init модуля повторно
        - события для Follow.module('name'): .beforeLoad(callback), .onReady(callback)
        - доступ к конфигурационному объекту модуля Follow.module('name').config = {model: '', storage: '', init: function(){}, ...}
        - состояние загруженности модуля Follow.module('name').status = "loaded | loading | not-exists"
        - при загрузке зависимостей модуля следить за тем, не были ли уже загружены файлы другим модулем
        - возможность указывать в качестве зависимостей для модуля не только внешние файлы, но также и другие модули (?)

Пометки для исправления доков:
    
    - @dependency переносится в js-объект
    - @key="//xpath" (если не задан то js:map будет создавать массив)
