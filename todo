TODO:

* изменить поведение аргумента if_not_defined до мода/флага со значениями:
 - "safe" - поведение как раньше при значении true
 - "forced" - вызов колбэков и трэкинг зависимостей даже в случае одинакового значения

* xml/xsl агрегатор данных для прокидывания в модель
* model.load(['path.to.first.collection', 'path.to.obj', 'etc'], '/js/modules/') - подгрузка внешних файлов для обработки данных модели (при их наличии)

* покрыть тестами основной функционал библиотеки (http://docs.jquery.com/QUnit)
